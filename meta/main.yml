---
dependencies:
  - role: common-server
    become: yes

  - role: logrotate
    become: yes

  - role: nvm
    nvm:
      user: "{{ ansible_user }}"
      version: master
      node_version: '6.3.0'
    become: yes
    tags: nvm

  - role: rvm_io.rvm1-ruby

  - role: common-monit-install
    become: yes
    tags: monit
